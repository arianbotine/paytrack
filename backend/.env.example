# ===========================================
# PayTrack Backend - Variáveis de Ambiente
# ===========================================
# Este arquivo é carregado automaticamente pelo NestJS
# Copie para .env: cp .env.example .env

# -------------------------------------------
# Banco de Dados
# -------------------------------------------
DATABASE_URL=postgresql://paytrack:paytrack123@localhost:5433/paytrack?schema=public

# -------------------------------------------
# Servidor
# -------------------------------------------
NODE_ENV=development
API_PORT=3000

# -------------------------------------------
# JWT Configuration
# -------------------------------------------
JWT_SECRET=super-secret-jwt-key-change-in-production
JWT_REFRESH_SECRET=super-secret-refresh-key-change-in-production
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# -------------------------------------------
# Performance & Cache
# -------------------------------------------
# Timeout de requisições HTTP (em milissegundos)
REQUEST_TIMEOUT_MS=30000

# TTL do cache do dashboard (em segundos)
CACHE_TTL_DASHBOARD=300

# TTL das chaves de idempotência (em segundos - 1 hora)
IDEMPOTENCY_TTL=3600

# -------------------------------------------
# Connection Pool do Banco de Dados
# -------------------------------------------
# Limite de conexões simultâneas (recomendado: 5-10 para ambientes gratuitos)
DATABASE_CONNECTION_LIMIT=10

# Timeout do pool em segundos
DATABASE_POOL_TIMEOUT=20

# -------------------------------------------
# CORS
# -------------------------------------------
# Origens permitidas (separadas por vírgula)
# CORS_ORIGINS=http://localhost:5173,https://meu-app.vercel.app
