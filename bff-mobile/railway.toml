[build]
builder = "DOCKERFILE"
dockerfilePath = "bff-mobile/Dockerfile"
dockerContext = "bff-mobile"

[deploy]
healthcheckPath = "/bff/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
