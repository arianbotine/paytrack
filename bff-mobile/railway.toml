[build]
builder = "DOCKERFILE"
dockerfilePath = "bff-mobile/Dockerfile.railway"

[deploy]
healthcheckPath = "/bff/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
