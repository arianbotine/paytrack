[build]
builder = "DOCKERFILE"
dockerfilePath = "bff-mobile/Dockerfile"

[deploy]
healthcheckPath = "/bff/health"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
